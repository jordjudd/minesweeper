<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Minesweeper</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; background-color: #f0f0f0; }
        .game-container { max-width: 600px; margin: 0 auto; text-align: center; }
        .game-info { margin: 20px 0; }
        .game-board { display: inline-block; border: 3px solid #999; background-color: #c0c0c0; }
        .cell { width: 30px; height: 30px; border: 2px outset #c0c0c0; background-color: #c0c0c0; 
                display: inline-block; text-align: center; line-height: 26px; font-weight: bold; 
                cursor: pointer; user-select: none; }
        .cell.revealed { border: 1px inset #c0c0c0; background-color: #e0e0e0; }
        .cell.mine { background-color: #ff0000; }
        .cell.flagged { background-color: #ffff00; }
        .row { display: block; height: 30px; }
        .controls { margin: 20px 0; }
        button { padding: 10px 20px; font-size: 16px; cursor: pointer; }
        .status { font-size: 18px; font-weight: bold; margin: 10px 0; }
        .won { color: green; }
        .lost { color: red; }
    </style>
</head>
<body>
    <div class="game-container">
        @RenderBody()
    </div>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    @RenderSection("Scripts", required: false)
</body>
</html>